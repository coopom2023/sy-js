/**
 * openstreetmap
 * @example
 * viewer.imageryLayers.addImageryProvider(new Cesium.OpenStreetMapImageryProvider({
 *   url: OSM_domain
 * }))
 */
export const OSM_domain = 'https://a.tile.openstreetmap.org'