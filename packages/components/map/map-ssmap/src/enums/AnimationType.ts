/**
 * 该项是动画类型的枚举说明
 * @example
 * let option = SSmap.AnimationType.VerticalFlow;
 */
export enum AnimationType {
  VerticalFlow = '垂直方向流动',
  HorizontalFlow = '水平方向流动',
  Twinkle = '渐变闪烁',
  Flash = '快速闪烁',
}
