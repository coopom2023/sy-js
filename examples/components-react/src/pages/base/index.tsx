import { UIButton as Button, UILink as Link } from '@sy-js/components-react'

function Base() {
  return (
    <div>
      <Button disabled>button</Button>
      <Link>link</Link>
    </div>
  )
}

export default Base
